/**
 * Archetype key type — kept for backward compatibility
 * (onboarding, plan engine, notifications still reference the key)
 */

export type ArchetypeKey = 'wolf' | 'bear' | 'lion' | 'deer' | 'tigress' | 'phoenix' | 'doe' | 'swan';
